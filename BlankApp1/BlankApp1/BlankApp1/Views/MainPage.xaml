<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Core.Views.MainPage"           	
             xmlns:cv="clr-namespace:Xamarin.Forms;assembly=Xamarin.Forms.CarouselView"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:b="clr-namespace:Corcav.Behaviors;assembly=Corcav.Behaviors"       
             xmlns:converters="clr-namespace:Core.Converters"
             xmlns:b1="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"  x:Name="MainPage" NavigationPage.HasNavigationBar="false" 
             ControlTemplate="{StaticResource header}">

    <ScrollView>
        <StackLayout>

            <!-- Title for square object-->
            <StackLayout HeightRequest="25"  HorizontalOptions="FillAndExpand">
                <Label FontAttributes="Bold"  FontSize="Large" Text="Advertenties" HorizontalOptions="Center"></Label>
            </StackLayout>
            
            <StackLayout VerticalOptions="StartAndExpand"  HorizontalOptions="FillAndExpand" HeightRequest="450" >
                <cv:CarouselView ItemsSource="{Binding NewsArticles}"  HeightRequest="400"  >
                    <cv:CarouselView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout BackgroundColor="Transparent" HeightRequest="400" >
                                <Image   HeightRequest="375" Aspect="AspectFill" Source="{Binding Image}"/>
                                <Label  HeightRequest="25"  Text="{Binding Name}" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
                                <Button Text="Lees meer" 
                                            Command="{Binding BindingContext.GetNewsArticleCommand,Source={x:Reference MainPage}}"
                                            CommandParameter="{Binding .}" />
                            </StackLayout>
                        </DataTemplate>
                    </cv:CarouselView.ItemTemplate>
                </cv:CarouselView>
            </StackLayout>


        <!-- Title for square object-->
            <StackLayout HeightRequest="25"  HorizontalOptions="FillAndExpand">
                <Label FontAttributes="Bold"  FontSize="Large" Text="Laatse nieuws" HorizontalOptions="Center"></Label> 
            </StackLayout>

            <StackLayout VerticalOptions="StartAndExpand"  HorizontalOptions="FillAndExpand" HeightRequest="450" >
                <cv:CarouselView ItemsSource="{Binding NewsArticles}"  HeightRequest="400"  >
                    <cv:CarouselView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout BackgroundColor="Transparent" HeightRequest="400" >
                                <Image   HeightRequest="375" Aspect="AspectFill" Source="{Binding Image}"/>
                                <Label  HeightRequest="25"  Text="{Binding Name}" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
                                <Button Text="Lees meer" 
                                            Command="{Binding BindingContext.GetNewsArticleCommand,Source={x:Reference MainPage}}"
                                            CommandParameter="{Binding .}" />
                            </StackLayout>
                        </DataTemplate>
                    </cv:CarouselView.ItemTemplate>
                </cv:CarouselView>
            </StackLayout>

            <!-- Title for square object-->
            <StackLayout HeightRequest="25"  HorizontalOptions="FillAndExpand">
                <Label FontAttributes="Bold" FontSize="Large"  Text="Deal focuss" HorizontalOptions="Center"></Label>
            </StackLayout>


            <StackLayout VerticalOptions="StartAndExpand"  HorizontalOptions="FillAndExpand" HeightRequest="450" >
                <cv:CarouselView ItemsSource="{Binding NewsArticles}"  HeightRequest="400"  >
                    <cv:CarouselView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout BackgroundColor="Transparent" HeightRequest="400" >
                                <Image   HeightRequest="375" Aspect="AspectFill" Source="{Binding Image}"/>
                                <Label  HeightRequest="25"  Text="{Binding Name}" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
                                <Button Text="Lees meer" 
                                            Command="{Binding BindingContext.GetNewsArticleCommand,Source={x:Reference MainPage}}"
                                            CommandParameter="{Binding .}" />
                            </StackLayout>
                        </DataTemplate>
                    </cv:CarouselView.ItemTemplate>
                </cv:CarouselView>
            </StackLayout>

            <!-- Title for square object-->
            <StackLayout HeightRequest="25"  HorizontalOptions="FillAndExpand">
                <Label FontAttributes="Bold" FontSize="Large" Text="Vestigingslocaties" HorizontalOptions="Center"></Label>
            </StackLayout>

            <StackLayout VerticalOptions="StartAndExpand"  HorizontalOptions="FillAndExpand" HeightRequest="450" >
                <cv:CarouselView ItemsSource="{Binding NewsArticles}"  HeightRequest="400"  >
                    <cv:CarouselView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout BackgroundColor="Transparent" HeightRequest="400" >
                                <Image   HeightRequest="375" Aspect="AspectFill" Source="{Binding Image}"/>
                                <Label  HeightRequest="25"  Text="{Binding Name}" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
                                <Button Text="Lees meer" 
                                            Command="{Binding BindingContext.GetNewsArticleCommand,Source={x:Reference MainPage}}"
                                            CommandParameter="{Binding .}" />
                            </StackLayout>
                        </DataTemplate>
                    </cv:CarouselView.ItemTemplate>
                </cv:CarouselView>
            </StackLayout>

            <!-- Title for square object-->
            <StackLayout HeightRequest="25"  HorizontalOptions="FillAndExpand">
                <Label FontAttributes="Bold" FontSize="Large" Text="Evenementen" HorizontalOptions="Center"></Label>
            </StackLayout>

            <StackLayout VerticalOptions="StartAndExpand"  HorizontalOptions="FillAndExpand" HeightRequest="450" >
                <cv:CarouselView ItemsSource="{Binding NewsArticles}"  HeightRequest="400"  >
                    <cv:CarouselView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout BackgroundColor="Transparent" HeightRequest="400" >
                                <Image   HeightRequest="375" Aspect="AspectFill" Source="{Binding Image}"/>
                                <Label  HeightRequest="25"  Text="{Binding Name}" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
                                <Button Text="Lees meer" 
                                            Command="{Binding BindingContext.GetNewsArticleCommand,Source={x:Reference MainPage}}"
                                            CommandParameter="{Binding .}" />
                            </StackLayout>
                        </DataTemplate>
                    </cv:CarouselView.ItemTemplate>
                </cv:CarouselView>
            </StackLayout>
            <!-- Title for square object-->
            <StackLayout HeightRequest="25"  HorizontalOptions="FillAndExpand">
                <Label FontAttributes="Bold" FontSize="Large" Text="Contentpartners" HorizontalOptions="Center"></Label>
            </StackLayout>

            <StackLayout VerticalOptions="StartAndExpand"  HorizontalOptions="FillAndExpand" HeightRequest="450" >
                <cv:CarouselView ItemsSource="{Binding NewsArticles}"  HeightRequest="400"  >
                    <cv:CarouselView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout BackgroundColor="Transparent" HeightRequest="400" >
                                <Image   HeightRequest="375" Aspect="AspectFill" Source="{Binding Image}"/>
                                <Label  HeightRequest="25"  Text="{Binding Name}" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
                                <Button Text="Lees meer" 
                                            Command="{Binding BindingContext.GetNewsArticleCommand,Source={x:Reference MainPage}}"
                                            CommandParameter="{Binding .}" />
                            </StackLayout>
                        </DataTemplate>
                    </cv:CarouselView.ItemTemplate>
                </cv:CarouselView>
            </StackLayout>


            <!-- Title for square object-->
            <StackLayout HeightRequest="25"  HorizontalOptions="FillAndExpand">
                <Label FontAttributes="Bold"  FontSize="Large" Text="Placeholder for Magzine | columnisten" HorizontalOptions="Center"></Label>
            </StackLayout>
            <!-- not done yet-->
            <StackLayout VerticalOptions="StartAndExpand"  HorizontalOptions="FillAndExpand" HeightRequest="450" BackgroundColor="Yellow">

                <Label  Text=""></Label>
            </StackLayout>
        </StackLayout>
    </ScrollView>
</ContentPage>